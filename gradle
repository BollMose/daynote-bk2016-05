# speedup android studio/gradle build
add some in local.properties:
org.gradle.daemon=true
org.gradle.jvmargs=-Xmx2048m -XX:MaxPermSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.configureondemand=true

# gradle plugin is too old please update to a more recent version ... 
res: change to classpath 'com.android.tools.build:gradle:1.5.0'
comment: downgrade the version of gradle, becuase of version of gradle and plugin is not the same.
