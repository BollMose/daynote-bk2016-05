

# View/onMeasure
from sdk docs: When overriding this method, you must call setMeasuredDimension(int, int) 
to store the measured width and height of this view. 

# GridLayoutManager
new GridLayoutManager(getActivity(),3), 2nd paramter is spanCount
